{
  "buildCommand": "npm run build --workspace @chatapp/web",
  "installCommand": "npm install",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
